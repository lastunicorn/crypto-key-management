<Window
  x:Class="DustInTheWind.SignatureManagement.Wpf.Main.MainWindow"
 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:signingPanel="clr-namespace:DustInTheWind.SignatureManagement.Wpf.Presentation.SigningPanel"
    xmlns:keysPanel="clr-namespace:DustInTheWind.SignatureManagement.Wpf.Presentation.KeysPanel"
   
    mc:Ignorable="d"
    Title="Signature Management - Keys"
    Height="600"
    Width="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="200"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*" MinWidth="400"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel - Keys Section -->
        <keysPanel:KeysPanelControl
            Grid.Column="0"
            DataContext="{Binding KeysPanelViewModel}"/>

        <!-- GridSplitter -->
        <GridSplitter
            Grid.Column="1"
            Width="5"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="LightGray"
            ShowsPreview="True"/>

        <!-- Right Panel - Message and Signature Section -->
        <signingPanel:SigningPanelControl
            Grid.Column="2"
            DataContext="{Binding SigningPanelViewModel}"/>

    </Grid>

</Window>