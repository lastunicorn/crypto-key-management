<Window
    x:Class="SignatureManagement.Wpf.Main.MainWindow"
 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:SignatureManagement.Wpf"
   
    mc:Ignorable="d"
    Title="Signature Management - Keys"
    Height="450"
    Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
            Text="Signature Keys"
            FontSize="16"
            FontWeight="Bold"
            Margin="10"
            HorizontalAlignment="Center"/>

        <Button Grid.Row="1"
            Name="CreateSampleKeyButton"
            Content="Create Sample Key"
            Margin="10,5"
            HorizontalAlignment="Center"
            Width="150"/>

        <ListBox Grid.Row="2"
            Name="SignatureKeysListBox"
            Margin="10"
            ItemsSource="{Binding SignatureKeys}">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5">
                        <TextBlock Text="{Binding Id}" FontWeight="Bold" FontSize="12"/>
                        <TextBlock Text="{Binding CreatedDateText}" FontSize="10" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>
    </Grid>

</Window>